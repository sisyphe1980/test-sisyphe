bitbake -c fetchall ma_recette
fetch - unpack - patch - licence - configure - compile - install -package - etc

bitbake -c listtasks ma_recette
bitbake -c listtasks ma_recette
bitbake -c listtasks ma_drecette

bitbake -c cleanstate ma_recette =+> supprime la recette +cache 

bitbake -c populate_sdk ==> temp/deploy/sdk

mkdir -p ../meta-myLayer/recipes-custom/my-script/script.bb 
                                                 /files/ici_mescript 
IMAGE_INSTALL_append = "my-script"

int phtread_create(pthread_t *thread, pthread_attr_t *attr, void *(*start_routine)(void *), void * arg); ==> le 2e arg permet de spécifier le comportement du thread
pthread_exit(void * retval);
pthread_exit(void * retval);

gcc -c fPIC truc.c -o truc.o
gcc -c fPIC truc.c -o truc.o

gcc -shared -fPIC truc.o -o libtruc.start_routine

CC = gcc
EXEC = prog
SRC = $(wildcard *.c)
OBJ = $(SRC:.c = .o)

all : $(EXEC)

$(EXEC) : $(SRC)
    $(CC) -Wall -g $^ -o $@

%.o : %.c
    $(CC) -c $< -o $@
 
